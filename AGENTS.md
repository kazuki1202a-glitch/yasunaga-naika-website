# このリポジトリを編集するAIへ

やすなが内科クリニック（福岡市早良区）の公式ホームページです。静的HTML/CSS/JS（フレームワークなし、ビルド不要）。GitHub Pagesで公開中（https://yasunaga-naika.com/）。このファイルはClaude・ChatGPT・Geminiなど、どのAIツールがこのリポジトリを編集する場合にも参照してください。

## 変更前に必ずすること

1. **今の内容を実際に読んでから変更する**（推測で書き換えない）
2. **どんな変更でも、実行する前に必ずチャットで「こう変更します」と内容を伝えて確認を取る。確認なしに勝手に実行しない。** 軽微な文言修正であっても同じ。承認が得られてから実行する
3. **変更後は何をどう変えたか、日本語で分かりやすく要約して伝える**
4. **話しかけてくる相手はITに詳しくない前提で応対する。** 「コミット」「デプロイ」「リポジトリ」のような専門用語は使わず、「保存」「反映」「サイトのファイル一式」のような普通の言葉で説明する。確認を求める時も、専門用語抜きで「こう変えます。これでいいですか？」のように聞く

## 軽微な変更（そのまま進めてOK）

- 文言の言い回し修正、誤字修正
- 料金・対象年齢など数字の修正（ただし依頼された数字をそのまま使う。自分で推測した数字を書かない）
- 改行位置の調整、余白などの微調整

## 複雑な変更（進める前に一度確認する）

以下に該当する場合は、**「この変更は複雑なので、直接ここで直すよりPC側での作業をおすすめします。それでも進めますか？」と一言確認してから**進めてください。

- レイアウト・配色・ページ構成に関わる変更
- 複数ページ・複数ファイルにまたがる変更
- ページの並び順・階層構造を変える変更
- 医療広告ガイドラインに関わりうる表現の追加

## 医療広告に関する禁止事項

以下のような表現は**追加してはいけません**（提案するだけに留め、実際に書き込まない）。
- 「必ず治る」「絶対に安全」など、効果を断定・保証する表現
- 「地域No.1」「最高の」「〇〇で一番」など、優良性を誤認させる比較表現
- 実在しない実績・症例数、誇張された体験談
- 院長・当院の実際の資格や実績と異なる記載

## セキュリティに関する禁止事項

- 外部サイトから読み込む`<script src="...">`タグの追加は禁止
- `onclick`など、インラインJavaScriptの追加は原則禁止
- 個人情報を入力させるフォームの追加は禁止（このサイトは電話予約のみの方針）

## このサイト特有のお約束

- **全ページ1ファイル**：`style.final.css`（スマホ用・全画面共通の基本スタイル）＋`style.pc.css`（PC=1024px以上専用。中身は全て`@media (min-width:1024px)`の中）の2つを読み込む構成。1ファイルで両方に対応しているので、スマホ版とPC版を別々に直す必要はない
- **CSS/JSを編集したら、影響する全HTMLファイルの`?v=N`（キャッシュ対策用のバージョン番号）を必ず1つ上げて、全ファイルで揃える。** 過去に上げ忘れてキャッシュ事故（直したのに反映されない）が起きたことがある
- **色は「内容」ではなく「並び順」に紐づく**：ページ内のタブ・セクションが複数ある箇所（例：ワクチンページの紺/オリーブ/金）は、1番目＝紺、2番目＝オリーブ、3番目＝金、という順番のルール。並び順を変える時は、色もこの順番に合わせて付け替える
- **パンくずリスト**：「診療案内」の子ページ（超音波検査・消化器内科など8ページ）には「ホーム＞診療案内＞◯◯」のパンくずがある。他のページ（施設基準・自由診療料金・プライバシーポリシー等）には付けない
- 診療内容・料金など医学的な内容は、実在するクリニックの実際の診療情報です。**事実と異なる内容を創作しない**

## 大きな変更の前に

ページ数が多い変更（全ページ一括の修正など）をする場合は、作業前に一式バックアップを取ってから進めてください。
